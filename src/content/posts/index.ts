import type { Post } from '@/types';

// telegram-as-the-fastest-frontend.mdx is drafted but not published yet:
// add its entry here (and rename the file to match the slug) to ship it.
export const posts: Post[] = [];
